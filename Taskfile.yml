# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  default:
    deps: [lint, test]
    silent: true
  lint:
    cmds:
      - golangci-lint run ./... --fix
  test:
    cmds: 
      - go test ./... -v
