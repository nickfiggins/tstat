version: '3'
tasks:
  default:
    deps: [lint, test]
    silent: true
  lint:
    cmds:
      - golangci-lint run ./... --fix
  test:
    cmds: 
      - go test ./... -v -coverprofile=cover.out
  cov:
    cmds:
    - go tool cover -html=cover.out -o cover.html
    - open cover.html
  doc:
    cmds: 
      - godoc -http=:6060
